<%= form_for(@player_session) do |f| %>
  <% if @player_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@player_session.errors.count, "error") %> prohibited this player_session from being saved:</h2>

      <ul>
      <% @player_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_id %><br />
    <%= f.number_field :player_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
